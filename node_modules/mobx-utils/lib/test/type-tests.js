"use strict";
var mobx_utils_1 = require("../src/mobx-utils");
{
    // test typings of fromPromise
    var x = { x: 3 };
    var p = mobx_utils_1.fromPromise(Promise.resolve(x));
    // p.value // compile error!
    if (p.state === mobx_utils_1.FULFILLED) {
        p.value.x = 4; // value only available if state is checked!
    }
}
